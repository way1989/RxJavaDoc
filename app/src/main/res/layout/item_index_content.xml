<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?selectableItemBackground"
    android:padding="8dp">

    <ImageView
        android:id="@+id/item_content_iv"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginEnd="10dp"
        android:scaleType="centerCrop"
        tools:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/item_content_thread"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/item_content_iv"
        android:lines="1"
        android:textSize="14sp"
        tools:text="Combin" />

    <TextView
        android:id="@+id/item_content_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/item_content_iv"
        android:layout_toEndOf="@+id/item_content_iv"
        android:layout_toStartOf="@+id/item_content_thread"
        android:lines="1"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        tools:text="Combin" />

    <TextView
        android:id="@+id/item_content_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/item_content_title"
        android:layout_marginTop="10dp"
        android:layout_toEndOf="@+id/item_content_iv"
        android:maxLines="2"
        android:textSize="14sp"
        tools:text="对序列的每一项都应用一个函数来变换发射的数据序列" />

</RelativeLayout>